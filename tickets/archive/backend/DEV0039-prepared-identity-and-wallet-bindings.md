# Ticket DEV0039: Prepared identity and wallet bindings

- Status: Completed
- Created: 2026-09-20
- Last updated: 2026-09-21
- Milestone: M0 identity / M2 wallet prerequisite
- Coordination: [COR0002 — Phantom authentication and demo access](../../current/organisatory/COR0002-phantom-auth-and-demo-access.md)
- Related records: depends on completed [DEV0038 — Phantom Supabase Web3 authentication](DEV0038-phantom-supabase-web3-authentication.md) and [DEV0015 — Supabase database foundation](DEV0015-supabase-database-foundation.md); enabled completed [DEV0040 — Protected access and database context](DEV0040-protected-access-and-database-context.md) and enables [DEV0041 — Club wallet authorization](../../current/backend/DEV0041-club-wallet-authorization.md)

## Objective and context

Map a verified Supabase Web3 subject to exactly one prepared RepX Club profile and active demo dataset through idempotent server-side enrollment. An active demo dataset is the server-selected `demo_runs` database row containing one isolated set of prepared users and product records; it is not a fitness activity or login session. Add the durable wallet-binding contract plus identity repositories needed to prevent arbitrary persona selection, wallet reuse and cross-dataset claims.

DEV0038 authenticates control of a Phantom address but deliberately grants no application identity. This ticket owns the next boundary from the specification's [demo account model](../../../docs/mvp-spec.md#3-demo-fixtures-and-account-model): controlled prepared enrollment and personal-wallet binding.

## Scope and non-goals

- In scope: define and migrate shared `wallet_bindings`; model active demo dataset, cluster, address, binding owner type, target and provenance; add Drizzle mappings and capability-owned identity/enrollment repositories; configure a server-only prepared roster that maps the verified wallet to Anna Klein and the active demo dataset; atomically and idempotently claim that seeded profile for the verified Supabase subject; enforce one wallet owner per dataset and one personal binding per prepared actor; expose a narrow enrollment/current-identity service and response contract; preserve an explicit wallet-disconnected state; test retries, races and wrong claims against real local PostgreSQL.
- Out of scope: an additional personal enrollment message or transaction signature; one-time `auth_challenges`, which DEV0041 will introduce for a distinct company wallet; Supabase provider sign-in/session implementation owned by DEV0038; private product-data row-level security and route guards owned by DEV0040; company-admin proof and company operations owned by DEV0041; open signup, persona picker, automatic wallet merging, production identity verification, payments, transaction signatures or chain state.

## Expected behavior and edge cases

After DEV0038 verifies the wallet and Supabase subject, the server privately maps that verified address to Anna Klein and the active demo dataset. The browser supplies no authoritative profile, dataset, role or owner type. On the first eligible request, one database transaction binds the Auth user, Anna's profile, her dataset participation record and personal wallet. A retry or simultaneous identical request returns the same identity instead of creating another binding. Supabase's verified wallet sign-in is the personal ownership proof; enrollment does not open a second Phantom prompt.

An unprepared address, a session whose verified wallet does not match the private roster, a different Auth user, an inactive dataset or participation record, or an already conflicting profile/wallet fails without partially claiming a profile. Concurrent claims converge on one matching result or a bounded conflict. A wallet already owned in the dataset cannot bind to another profile or organization; a profile or Auth user cannot silently acquire a second personal identity.

The user may disconnect Phantom at any time through the existing wallet control. Disconnecting does not sign out the Supabase session, delete Anna's profile or remove/reassign the durable wallet binding. The interface shows that the wallet is disconnected, and later wallet-signing or payment actions remain unavailable until the same bound wallet reconnects. Connecting a different wallet produces an explicit mismatch and never changes Anna's binding. Public wallet addresses and enrollment metadata may be stored; signatures, private keys and recovery phrases may not. Error responses use bounded application copy and do not leak the prepared roster or identify whether an arbitrary address is allowlisted.

## Assumptions, decisions, and dependencies

Completed DEV0038 records the real Supabase subject/session and signer-adapter contracts, satisfying this ticket's authentication dependency. DEV0015 already provides `auth.users`, `profiles`, `demo_runs` and membership tables plus default-deny row-level security. Extend that schema through a new replayable migration; never rewrite the completed foundation migration.

One shared `wallet_bindings` contract must support mutually exclusive personal and organization ownership so the same `(demo dataset, cluster, address)` can have exactly one owner. This ticket owns that shared table and its personal enrollment path. DEV0041 owns the one-time `auth_challenges` table, organization-binding creation/use and company proof behavior. Record the exact `wallet_bindings` column/check/index matrix in this ticket before the first migration edit.

The user selected Anna Klein as the prepared profile and confirmed that the already verified Supabase wallet proof is sufficient for personal enrollment, with no second Phantom prompt. The prepared roster is server-only configuration. Public wallet addresses are not secrets, but the local address-to-Anna mapping remains an ignored environment/config input rather than a committed fixture. Never record seed phrases or signing material.

### Locked implementation contracts — 2026-09-21

`app.wallet_bindings` has the following exact migration contract before implementation begins:

| Column                                | Contract                                                                                                                                                                                     |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                                  | UUID primary key with `gen_random_uuid()` default.                                                                                                                                           |
| `run_id`                              | Required UUID referencing `app.demo_runs`; this identifies the isolated demo dataset.                                                                                                        |
| `cluster`                             | Required text; MVP check permits only `solana:devnet`.                                                                                                                                       |
| `wallet_address`                      | Required 32–44 character base58 text; server configuration additionally validates it with the installed Solana address codec.                                                                |
| `owner_type`                          | Required text, either `personal` or `organization`.                                                                                                                                          |
| `profile_id` / `organization_id`      | Mutually exclusive nullable targets. Personal rows require the profile and forbid the organization; organization rows do the reverse. Composite foreign keys keep either target in `run_id`. |
| `bound_by_auth_user_id`               | Required UUID referencing `auth.users`; records the verified Supabase subject that established the binding.                                                                                  |
| `provenance`                          | Required text; this MVP permits `prepared`.                                                                                                                                                  |
| `status`, `verified_at`, `revoked_at` | Status is `active` or `revoked`; `verified_at` is required; `revoked_at` is present exactly for revoked rows.                                                                                |
| `created_at`, `updated_at`            | Required audit timestamps with the shared update trigger.                                                                                                                                    |

Partial unique indexes enforce one active owner for `(run_id, cluster, wallet_address)`, one active personal wallet for `(run_id, profile_id)`, one active organization wallet for `(run_id, organization_id)`, and one active personal identity for `(run_id, bound_by_auth_user_id)`. A lookup index covers subject, status and run. Forced row-level security remains enabled with no general runtime read/write policy.

Two `SECURITY DEFINER` functions owned by the non-login `app_owner` role form the only DEV0039 runtime database surface: `app.enroll_prepared_personal_identity(...)` atomically claims a fixture profile and inserts or returns its exact active binding; `app.current_prepared_personal_identity(...)` reads that same exact mapping without mutation. Both require Auth subject, run slug, profile slug, cluster and wallet address supplied by trusted server code. Narrow `app_owner` policies activate only inside these functions through a transaction-local marker; direct `app_runtime` table access remains default-denied. Function execution is granted only to `app_runtime`, with public, `anon`, `authenticated` and `service_role` access revoked.

Server-only `PREPARED_PERSONAL_IDENTITIES_JSON` is a bounded JSON array of `{ walletAddress, profileSlug, demoRunSlug, cluster }`. Entries must contain valid Solana addresses, fixture-style slugs and `solana:devnet`; duplicate wallet or profile/run mappings fail configuration. The local ignored value maps the prepared Phantom address to `anna-klein` and `local-foundation-2030`. No private key or signature is stored.

`GET /api/auth/identity` reads the current mapping; `POST /api/auth/identity` performs idempotent enrollment. Both derive the subject/address from `verifiedAuthSession()`. POST accepts no identity body and requires the configured same origin. Responses are bounded `enrolled`, `not-enrolled`, `not-prepared`, `signed-out` or `unavailable` states with no unrestricted roster data. The sign-in screen automatically calls POST once for each verified subject/wallet and displays Anna after success; reload safely repeats it. Provider disconnect changes only live wallet availability, not the saved binding or Supabase session.

The migration is forward-only. Disposable local rollback uses `npm run db:reset`; any applied hosted correction uses a new migration rather than rewriting history. Local application testing provisions a loopback-only login inheriting `app_runtime`; hosted credentials remain environment-managed and are never committed.

## Implementation plan

1. Read DEV0038's delivered verified-session contract and write the exact `wallet_bindings` schema, uniqueness, role/grant and rollback matrix before migration changes.
2. Add a forward-only migration, Drizzle mappings and database tests for owner exclusivity, personal-binding uniqueness, idempotent retries and concurrent claims.
3. Add server-only prepared-roster parsing plus identity/enrollment repositories under the established `src/server` dependency direction. Derive Anna and the active demo dataset from the verified session wallet and server configuration, validate every identifier server-side and keep enrollment atomic.
4. Add narrow enrollment/current-identity services and framework adapters. Reuse the verified DEV0038 subject/address without requesting another signature and never trust browser-selected identity metadata.
5. Add unit/integration/browser evidence for successful prepared enrollment, disallowed/mismatched wallets, wrong user/dataset/profile, concurrent retry recovery, wallet disconnect, same-wallet reconnect and different-wallet mismatch.

## Acceptance criteria

- [x] AC1: The verified prepared wallet atomically enrolls as Anna Klein in the server-selected active demo dataset and returns the same identity on retries or simultaneous identical requests.
- [x] AC2: A wallet has exactly one personal or organization owner per dataset/cluster; one Auth subject/profile cannot be silently merged with another prepared actor.
- [x] AC3: Personal enrollment relies on the verified DEV0038 Supabase subject/wallet and private roster without a second Phantom signature. Disallowed, mismatched or conflicting inputs produce no partial enrollment.
- [x] AC4: The server derives Anna, the active demo dataset and personal owner type from protected configuration and verified session data; the client cannot select them as authority.
- [x] AC5: Disconnecting Phantom preserves the Supabase session and durable Anna binding while blocking wallet-required actions. Reconnecting the same wallet restores the match; a different wallet cannot replace or merge the binding.
- [x] AC6: Migration, Drizzle mapping, repository, response and server-only configuration contracts are documented with rollback/recovery steps. No private key, recovery phrase, signature or unrestricted roster data reaches the browser.
- [x] AC7: Clean database reset, repeated migration/seed, SQL and driver integration tests, unit/boundary checks, lint, typecheck, build and affected browser checks pass with exact results recorded.

## Validation plan

Use local Supabase/PostgreSQL with prepared test identities, at least two distinct wallets, one disallowed wallet, one inactive dataset participation and concurrent attempts at the same profile. Validate constraints directly with SQL/pgTAP and through the Drizzle repositories. Include wrong-dataset/user/profile/wallet mappings, repeated requests and conflicting concurrent claims.

Run clean reset and repeatability checks, database integration tests, existing unit/boundary tests, lint, typecheck, formatting, webpack production build and affected Playwright flows. Rehearse one real prepared Phantom enrollment after DEV0038 sign-in, then disconnect, reload, reconnect the same wallet and connect a different account. Confirm enrollment causes no second message prompt and never constructs or signs a transaction.

## Implementation record

Implementation and validation are complete. The user confirmed the real prepared-Phantom enrollment, reload, disconnect and same-wallet reconnect behavior on 2026-09-21. This ticket was created when the unimplemented DEV0016 plan was converted to COR0002. It owns the shared binding schema and personal prepared-enrollment behavior.

### Changes and rationale

Added a forward-only `app.wallet_bindings` migration with mutually exclusive personal/organization targets, active-owner uniqueness, audit/revocation state, forced row-level security and a shared table shape that DEV0041 can extend without creating a second ownership model. Runtime table grants remain unusable without a matching policy. The only DEV0039 database capabilities are two `SECURITY DEFINER` functions owned by `app_owner`: one atomically enrolls a prepared personal identity and the other returns that exact current identity. Their transaction-local marker enables six narrow owner policies; direct `app_runtime` reads remain empty.

Added the Drizzle mapping and a server-only repository over those functions. A bounded server-only roster parser accepts at most 20 exact Devnet mappings, validates Solana addresses and fixture slugs, and rejects duplicate wallets or actors. The identity service derives the roster entry from the verified Supabase session wallet, never from browser-submitted profile/run data. Conflicts return a bounded `not-prepared` result; missing or invalid server configuration fails as `unavailable` rather than misreporting a wallet as disallowed.

Added no-body `GET`/`POST /api/auth/identity` adapters, a strict browser response parser and a small browser client. POST requires the configured same origin and reads no profile, run, role, owner type or wallet field from the request. The sign-in screen automatically performs idempotent enrollment once per verified subject/wallet, displays Anna on success and states that no second Phantom signature was used. It now exposes an explicit `Disconnect Phantom` action. A disconnect preserves the Supabase session and durable identity result; the interface requires the same wallet before later signing actions and keeps a different connected wallet in the existing mismatch state.

Added a loopback-only local runtime-login provisioner because the application must exercise forced row-level security through a non-bypass login rather than the local database owner. Setup documentation distinguishes its disposable fixed local password from generated hosted credentials. Unit, pgTAP, real-driver and browser regression coverage now verifies roster/response bounds, default-deny access, exact constraints, inactive-participant denial, no partial writes, simultaneous retries, subject conflicts and signed-out request behavior.

### Affected files

| File or component                                                                                                                                                                                                                                                                                                                                                     | Change and purpose                                                                                                                                                        |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`supabase/migrations/20260921000100_create_wallet_bindings.sql`](../../../supabase/migrations/20260921000100_create_wallet_bindings.sql)                                                                                                                                                                                                                             | Creates the binding table, indexes, forced-RLS policies and narrow personal enrollment/current-identity functions.                                                        |
| [`src/server/db/schema/identity.ts`](../../../src/server/db/schema/identity.ts), [`src/server/db/schema/index.ts`](../../../src/server/db/schema/index.ts)                                                                                                                                                                                                            | Maps and exports the exact binding schema for trusted server use.                                                                                                         |
| [`src/server/db/identity/repository.ts`](../../../src/server/db/identity/repository.ts)                                                                                                                                                                                                                                                                               | Calls only the two database capability functions, maps their bounded result and normalizes database conflicts.                                                            |
| Historical `src/server/identity/config.ts` and `src/server/identity/env.ts`; [`src/server/identity/service.ts`](../../../src/server/identity/service.ts)                                                                                                                                           | Validates private prepared mappings, matches the verified session wallet and exposes enrollment/current-identity services.                                                |
| [`src/auth/identity-contracts.ts`](../../../src/auth/identity-contracts.ts), [`src/auth/client/identity-client.ts`](../../../src/auth/client/identity-client.ts)                                                                                                                                                                                                      | Defines an exact browser-safe response union and fail-closed GET/POST client.                                                                                             |
| [`src/app/api/auth/identity/route.ts`](../../../src/app/api/auth/identity/route.ts)                                                                                                                                                                                                                                                                                   | Adds same-origin, no-body enrollment and current-identity HTTP adapters over the verified session.                                                                        |
| [`src/features/auth/sign-in.tsx`](../../../src/features/auth/sign-in.tsx)                                                                                                                                                                                                                                                                                             | Runs enrollment without another wallet prompt, presents Anna/error/disconnect/mismatch states and adds explicit wallet disconnect.                                        |
| [`scripts/prepare-local-runtime-database.mjs`](../../../scripts/prepare-local-runtime-database.mjs), [`package.json`](../../../package.json), [`.env.example`](../../../.env.example)                                                                                                                                                                                 | Provisions the disposable restricted login, adds `db:runtime` and documents the server-only roster variable.                                                              |
| [`README.md`](../../../README.md), [`supabase/README.md`](../../../supabase/README.md), [`docs/mvp-spec.md`](../../../docs/mvp-spec.md)                                                                                                                                                                                                                               | Documents local setup, the implemented identity boundary, disconnect behavior and remaining protected-access work.                                                        |
| [`supabase/tests/database/identity.test.sql`](../../../supabase/tests/database/identity.test.sql), [`supabase/tests/database/foundation.test.sql`](../../../supabase/tests/database/foundation.test.sql)                                                                                                                                                              | Covers the extension schema and keeps the completed foundation checks scoped to foundation-owned objects.                                                                 |
| [`tests/identity.test.ts`](../../../tests/identity.test.ts), [`tests/database/identity.test.ts`](../../../tests/database/identity.test.ts), [`tests/boundaries.test.ts`](../../../tests/boundaries.test.ts), [`tests/supabase-auth-config.test.ts`](../../../tests/supabase-auth-config.test.ts), [`tests/browser/auth.spec.ts`](../../../tests/browser/auth.spec.ts) | Covers configuration and response parsing, real restricted-driver concurrency/conflicts, server-only boundaries, setup/source invariants and signed-out browser behavior. |

Replacement note, 2026-09-21: completed [DEV0046](DEV0046-email-otp-registration-and-application-profiles.md) removed the two historical prepared-roster files named above when open email enrollment replaced automatic Anna enrollment. Their names remain here as implementation history rather than live source links.

### Decisions and deviations

The original plan required a separate one-time application challenge for personal enrollment. On 2026-09-21 the user chose the simpler flow: the successful DEV0038 Supabase wallet proof plus a private server mapping is sufficient to bind the prepared wallet to Anna Klein. `auth_challenges` moved to DEV0041 because a fresh proof is still necessary when an admin intentionally switches from a personal wallet to a different company wallet. The durable personal binding survives a provider disconnect; only live wallet-required authority becomes unavailable.

The implementation uses narrow database functions instead of granting a general application repository access to private identity tables. This keeps forced row-level security effective for direct runtime queries while allowing one reviewed transaction to lock/claim the prepared fixture and create or return its exact binding. Identical concurrent requests serialize on the profile lock and converge on the same row; a different subject reaches a bounded conflict.

No second message, transaction, balance or RPC operation was added. The server configuration error state is intentionally distinct from an unprepared wallet internally and becomes browser-safe `unavailable` copy; neither response exposes the roster. The HTTP POST accepts an empty body only so future browser fields cannot accidentally become identity authority.

### Contracts, configuration, and operations

The delivered database, uniqueness and function contracts match the locked matrix above. The migration is forward-only. Before deployment, apply it through the normal Supabase migration path and provide an environment-managed login that inherits `app_runtime` without superuser or row-level-security bypass privileges. Hosted passwords must be generated secrets.

For disposable local use after each `db:reset` or first `auth:start`, run `npm run db:runtime`. Set server-only `DATABASE_URL` to that loopback login and set `PREPARED_PERSONAL_IDENTITIES_JSON` to the exact JSON array documented above. The ignored local value maps the user's prepared public address to `anna-klein` in `local-foundation-2030`; no key, recovery phrase or signature is stored. The variables have no `NEXT_PUBLIC_` prefix and are not returned by the API.

`POST /api/auth/identity` is idempotent enrollment; `GET /api/auth/identity` reads an existing exact binding. Both obtain subject/address exclusively from `verifiedAuthSession()`. Responses contain only `status`, and for an enrolled identity the bounded profile, demo-run, role and wallet-binding snapshot. Disposable local rollback is `npm run db:reset` followed by `npm run db:runtime`; an applied hosted correction requires a new migration.

## Validation results

Automated implementation validation and the real prepared-wallet rehearsal are complete. On 2026-09-21 the user approved one fresh Supabase login message, observed Anna's successful enrollment without a second Phantom prompt, reloaded successfully, disconnected while the Anna session remained active, reloaded in the disconnected state and reconnected the same wallet successfully. A read-only database check then found exactly one active binding, for Anna, owned by one Auth subject; it printed no wallet address or session data.

| Criterion | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Result                |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| AC1       | `npm run db:test` exercises repeat enrollment; `npm run test:db` starts two simultaneous runtime-login calls and confirms one binding plus identical repository retry/current results.                                                                                                                                                                                                                                                                                                                  | Passed automatically. |
| AC2       | pgTAP verifies target checks and all four partial unique ownership indexes; the driver conflict check leaves the original subject and single binding unchanged.                                                                                                                                                                                                                                                                                                                                         | Passed automatically. |
| AC3       | Roster unit tests reject disallowed/malformed/duplicate mappings; pgTAP rejects inactive participation without partial writes; source and route checks prove no request identity body or signing adapter is used. The real rehearsal produced only the Supabase login prompt and no second enrollment prompt.                                                                                                                                                                                           | Passed.               |
| AC4       | Server-only boundary tests cover roster, service and repository; POST accepts no body; exact response parsing rejects extra roster/authority fields.                                                                                                                                                                                                                                                                                                                                                    | Passed automatically. |
| AC5       | Existing relationship unit tests cover matched/disconnected/mismatched states and the database prevents reassignment. The real rehearsal preserved Anna and the Supabase session across disconnect/reload, then restored the matched state when the same wallet reconnected.                                                                                                                                                                                                                            | Passed.               |
| AC6       | Migration, Drizzle, repository, response, environment, local-login and rollback contracts are documented here, in `.env.example`, README and the database operations guide.                                                                                                                                                                                                                                                                                                                             | Passed.               |
| AC7       | Two consecutive `npm run db:reset` runs applied both migrations and seed successfully; `npm run db:runtime` passed after each. `npm run db:test` passed 43/43, `npm run test:db` 8/8, `npm test` 40/40, `npm run db:lint` with no schema errors, `npm run lint`, `npm run typecheck`, `npm run format:check`, webpack production build and `npm run test:e2e` 34/34 across desktop/mobile. The final build's affected sign-in subset then passed 4/4, followed by the successful real-wallet rehearsal. | Passed.               |

The first database-lint run identified an unused PL/pgSQL variable and an incorrect `STABLE` marker on a function that uses transaction-local configuration; both were removed before clean-reset validation. An early pgTAP failure came from invoking the test assertion itself while the restricted runtime role was active, not from the migration. Moving the assertion outside that role preserved the intended direct-access check, after which all SQL tests passed.

Completion/archive validation found 38 unique work records with valid lifecycle status and index paths, and resolved 600 repository-local links across the 58 Markdown files in this commit snapshot. Focused Prettier formatting and `git diff --check` passed.

## Risks, limitations, and follow-ups

Prepared allowlisting is a bounded demo identity mechanism, not proof of one human or a production signup design. Wallet uniqueness does not prevent one person from owning multiple addresses. Reusing the verified Supabase session avoids a second proof-of-possession prompt; therefore session verification, private server-derived mapping, transaction atomicity and uniqueness constraints are the personal-enrollment security boundary.

DEV0041 must consume the shared exclusivity contract instead of adding a competing company-wallet table. DEV0040 must consume the verified identity result instead of trusting client profile/run claims.

The local runtime helper intentionally contains a fixed password only for the disposable loopback database on port 55322. It is not a hosted credential design. Product private-data policies remain out of scope until DEV0040; enrolling Anna does not yet authorize catalogue drafts, payments or any transaction.

## Completion and review references

- Completed: 2026-09-21 — implementation, automated validation and the real prepared-wallet enrollment/disconnect/reconnect rehearsal passed.
- Commits: `f64a5db` (`[DEV0039] Implement prepared identity enrollment`) and `[DEV0039] Complete prepared identity enrollment`.
- Review: Implementation self-review and user acceptance rehearsal completed; no independent code review.
- Deployment or release: None.
