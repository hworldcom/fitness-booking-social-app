# Ticket DEV0015: Supabase database foundation

- Status: Completed
- Created: 2026-09-19
- Last updated: 2026-09-20
- Milestone: M0 database foundation
- Coordination: [COR0001 — Project structure](../../current/organisatory/COR0001-project-structure.md)
- Related tickets: [DEV0014 — Planning record](DEV0014-database-and-backend-plan.md), [DEV0008 — Frontend](../frontend/DEV0008-repx-club-frontend.md); implemented the server boundary with [DEV0025](DEV0025-nextjs-backend-boundary.md); next [DEV0016](../../current/backend/DEV0016-phantom-auth-and-demo-access.md)

## Objective and context

Establish repeatable PostgreSQL schema, secure runtime access and seeded catalogue data before connecting the frontend to a shared backend. Implement the [database recommendation and phases](../../../docs/mvp-spec.md#database-provider-recommendation--19-september-2026) and [migration contract](../../../docs/mvp-spec.md#migrations-environments-and-seed-boundaries). Supabase is the recommended provider, not yet a provisioned account.

## Scope and non-goals

- In scope: pinned Supabase local tooling, Drizzle/Postgres driver, the sole `supabase` configuration/migration/policy/seed history, server-only environment/connection modules and Drizzle table mappings under `src/server/db`, separate runtime/migration roles, base identity/run/organization/venue/class/membership tables, constraints/indexes and deterministic catalogue seeds; redacted environment example and real setup/test commands.
- Out of scope: the general server import/layer enforcement owned by DEV0025; browser sign-in, exposing private business APIs, migrating localStorage, challenge settlement/financial tables, payments, visits, event publication, production deployment or paid upgrades. Do not rewrite the frontend or create every future table now.

## Expected behavior and edge cases

A clean local database can be migrated and seeded repeatedly without duplicate catalogue entities. A runtime connection cannot modify schema, bypass row policies or access private rows without valid context. Unclaimed demo profiles have no login/role authority. Organization and venue types support cafés as well as gyms; trainer affiliations can span venues. Every run-scoped relationship rejects cross-run references. Missing/malformed configuration fails explicitly without logging credentials. SQL constraints reject negative/excessive amounts, invalid times and duplicate membership identities where those fields exist.

## Assumptions, decisions, and dependencies

Implementation was authorized on 2026-09-20 after the schema and ownership review. Begin locally; no hosted Supabase project, region or paid plan is required to implement and validate this foundation. The pinned CLI runs through the project package manager and requires a running Docker-compatible container runtime. A hosted project remains a later setup input with separately supplied secrets and must not be inferred from local completion.

Use `supabase/migrations/` as the only applied migration history and Drizzle for typed server queries. Runtime app tables live in an unexposed `app` schema, with default-deny row-level security (RLS) and least-privilege roles; policies for authenticated feature access arrive in subsequent slices. Implement [DEV0025](DEV0025-nextjs-backend-boundary.md) concurrently from the first server configuration/database edit. DEV0015 owns database dependencies, SQL artifacts, server-only connection configuration, Drizzle table mappings and database integration evidence. DEV0025 owns the server-only/import rules that constrain them. Feature tickets own the repositories and services that query those mappings; DEV0015 must not pre-build catalogue, identity, draft or booking repositories.

The completed [DEV0031 boundary](../frontend/DEV0031-preview-data-and-domain-boundaries.md) is the frontend baseline. Database rows and inferred Drizzle row types stay server-only. Later repositories map selected rows into explicit `src/domain` or response contracts; they never import `src/features/preview`, expose raw row types to features, or silently replace preview data.

The DEV0015 local start command launches PostgreSQL only. Auth, Realtime, Storage, Studio, the Data API and other auxiliary services are outside this ticket and are enabled by the feature ticket that first needs them. This keeps database validation repeatable alongside another running Supabase project and avoids treating unrelated service health as schema evidence. The same-database seed command uses the checked-in Postgres.js dependency against the fixed loopback development port because Supabase CLI 2.117.0 no longer applies general SQL through `supabase seed --local`; it must never accept or target a hosted connection.

## Foundation schema contract

### Shared SQL conventions

- All application tables use the `app` schema. Entity tables use UUID primary keys generated with `gen_random_uuid()`; relationship tables use the composite UUID business keys stated below. Audit and interval fields use `timestamptz`; `created_at`/`updated_at` default to `now()` and a shared trigger advances `updated_at` on change. Slugs are lowercase kebab-case checked by `^[a-z0-9]+(?:-[a-z0-9]+)*$`. Core run/catalogue records are retired or disabled through status fields rather than hard-deleted.
- Every run-scoped entity with its own UUID identifier also has `unique (run_id, id)`; relationship tables use the composite business key as their primary key. Relationships that must stay in one run use composite foreign keys containing `run_id`; a globally unique UUID alone is not sufficient proof of run membership.
- Activity values are limited to `Running`, `Strength`, `Muay Thai` and `Yoga`. Venue activity arrays are non-empty and may contain only those values. Status/kind/role columns use `text` plus named `check` constraints in this foundation rather than PostgreSQL enums, so later additions use ordinary reviewed migrations.
- Monetary values use `numeric(20,0)` base units and must be between `0` and `18446744073709551615`, inclusive. Drizzle/server boundaries serialize them as decimal strings and never JavaScript numbers. DEV0015 stores only class catalogue prices; it creates no balance, payment, refund, pool or ledger rows.
- Scheduled records store UTC `timestamptz` instants plus an IANA timezone snapshot. Database checks enforce `ends_at > starts_at`; presentation derives venue-local dates/times outside SQL storage contracts.
- All foreign keys below use `on delete restrict` unless a later owning ticket records a different lifecycle before its migration. Required lookup/foreign-key indexes are explicit rather than assumed from constraints.

### Exact table matrix

| Table                          | Columns and nullability                                                                                                                                                                                                                                                                                                                                                                                        | Keys, checks and foreign keys                                                                                                                                                                                                                                                                   | Required indexes and seed boundary                                                                                                                                                                                                                |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app.profiles`                 | `id uuid PK`; `auth_user_id uuid null`; `slug text not null`; `display_name text not null`; `initials text not null`; `bio text not null default ''`; `avatar_color text not null`; `record_source text not null`; `claimed_at timestamptz null`; `created_at`, `updated_at timestamptz not null`                                                                                                              | `auth_user_id -> auth.users(id)`; unique `auth_user_id` and `slug`; source in `fixture,user`; display name 2–80 chars, initials 1–4 chars; `auth_user_id` and `claimed_at` are both null or both non-null. Auth deletion is restricted so a claimed profile cannot silently become claimable.   | Unique indexes cover auth and slug lookup. Seed Anna, Daniel, Lea, Max, Kru Sam and Maya Fischer as `fixture`, unclaimed (`auth_user_id`/`claimed_at` null).                                                                                      |
| `app.demo_runs`                | `id uuid PK`; `slug`, `name text not null`; `status text not null`; `catalogue_visibility text not null`; `schedule_anchor_date date not null`; `starts_at`, `ends_at timestamptz not null`; `retired_at timestamptz null`; `created_at`, `updated_at timestamptz not null`                                                                                                                                    | Unique slug; status in `prepared,active,retired`; visibility in `private,public`; `ends_at > starts_at`; `retired_at` required only for retired status. A partial unique index permits at most one `active/public` run.                                                                         | Index `(status, catalogue_visibility)`. Seed one deterministic local run with an explicit anchor date; later run-creation tooling supplies a new future anchor instead of rewriting fixed preview dates.                                          |
| `app.demo_run_memberships`     | `run_id`, `profile_id uuid not null`; `role`, `status text not null`; `joined_at timestamptz not null`; `revoked_at timestamptz null`; `created_at`, `updated_at timestamptz not null`                                                                                                                                                                                                                         | PK `(run_id, profile_id)`; FKs to `demo_runs(id)` and `profiles(id)`; role in `member,operator`; status in `active,revoked`; revoked timestamp matches revoked status. Membership alone grants no Auth identity.                                                                                | Index `(profile_id, status, run_id)`. Seed memberships for fixture profiles; unclaimed members cannot authenticate or exercise roles.                                                                                                             |
| `app.organizations`            | `id`, `run_id uuid not null`; `slug`, `name`, `description text not null`; `kind`, `status`, `record_source text not null`; `created_at`, `updated_at timestamptz not null`                                                                                                                                                                                                                                    | PK `id`; unique `(run_id,id)` and `(run_id,slug)`; `run_id -> demo_runs`; kind in `gym,cafe,studio,community,sponsor`; status in `active,inactive`; source in `fixture,user`.                                                                                                                   | Index `(run_id, kind, status)`. Seed the organisations that own Kru Tiger, Fabrik Training, Studio Vela and Sunday Coffee; no wallet or financial authority is created.                                                                           |
| `app.organization_memberships` | `run_id`, `organization_id`, `profile_id uuid not null`; `role`, `status text not null`; `created_at`, `updated_at timestamptz not null`; `revoked_at timestamptz null`                                                                                                                                                                                                                                        | PK `(run_id, organization_id, profile_id)`; composite FKs to organizations and run memberships; role in `primary_admin,admin,member`; status in `active,revoked`; revoked timestamp matches revoked status. Organization membership does not imply venue staff or company-wallet authority.     | Index `(run_id, profile_id, status)`. Seed only relationships required by prepared personas; no browser-selectable role assignment.                                                                                                               |
| `app.venues`                   | `id`, `run_id`, `organization_id uuid not null`; `slug`, `name`, `area`, `city`, `country_code`, `timezone`, `description text not null`; `kind`, `status`, `record_source text not null`; `activity_tags text[] not null`; `created_at`, `updated_at timestamptz not null`                                                                                                                                    | PK `id`; unique `(run_id,id)` and `(run_id,slug)`; composite FK `(run_id,organization_id)`; kind in `gym,studio,cafe,outdoor,other`; two-letter uppercase country; non-empty allowed activity array; status/source checks.                                                                      | Index `(run_id, kind, status)` and GIN on `activity_tags`. Seed Kru Tiger, Fabrik Training, Studio Vela and Sunday Coffee with `Europe/Berlin`; venue type is not forced to gym.                                                                  |
| `app.venue_staff`              | `run_id`, `venue_id`, `profile_id uuid not null`; `role`, `status text not null`; `created_at`, `updated_at timestamptz not null`; `revoked_at timestamptz null`                                                                                                                                                                                                                                               | PK `(run_id, venue_id, profile_id)`; composite FKs to venues and run memberships; role in `manager,check_in_staff`; status in `active,revoked`; revoked timestamp matches revoked status. Staff scope is exactly one assigned venue and conveys no financial authority.                         | Index `(run_id, profile_id, status)`. Seed only the prepared venue/operator relationship needed by later attendance work.                                                                                                                         |
| `app.trainer_affiliations`     | `run_id`, `venue_id`, `profile_id uuid not null`; `title text not null`; `activity_tags text[] not null`; `status text not null`; `created_at`, `updated_at timestamptz not null`                                                                                                                                                                                                                              | PK `(run_id, venue_id, profile_id)`; composite FKs to venues and run memberships; non-empty allowed activity array; status in `active,inactive`. A profile may have affiliations at multiple venues.                                                                                            | Index `(run_id, profile_id, status)` and GIN on `activity_tags`. Seed Kru Sam/Kru Tiger, Maya/Fabrik and Lea/Studio Vela affiliations.                                                                                                            |
| `app.class_sessions`           | `id`, `run_id`, `venue_id`, `trainer_profile_id uuid not null`; `slug`, `title`, `description`, `discipline`, `timezone`, `currency_code text not null`; `starts_at`, `ends_at timestamptz not null`; `capacity integer not null`; `price_base_units numeric(20,0) not null`; `membership_eligible boolean not null`; `status`, `record_source text not null`; `created_at`, `updated_at timestamptz not null` | PK `id`; unique `(run_id,id)` and `(run_id,slug)`; composite FK to venue and `(run_id,venue_id,trainer_profile_id)` affiliation; allowed discipline; `ends_at > starts_at`; `capacity > 0`; exact unsigned-range price; currency exactly `EURC`; status in `scheduled,cancelled`; source check. | Index `(run_id, starts_at)`, `(run_id, venue_id, starts_at)` and `(run_id, discipline, starts_at)`. Seed the three catalogue classes from explicit run-anchor offsets; preserve labels/prices but do not claim live availability or reservations. |
| `app.membership_entitlements`  | `id`, `run_id`, `profile_id`, `venue_id uuid not null`; `label text not null`; `starts_at`, `ends_at timestamptz not null`; `status`, `record_source text not null`; `created_at`, `updated_at timestamptz not null`                                                                                                                                                                                           | PK `id`; unique `(run_id,id)` and `(run_id,profile_id,venue_id,starts_at)`; composite FKs to run membership and venue; `ends_at > starts_at`; status in `active,revoked,expired`; source check. An entitlement is venue-scoped and does not itself reserve capacity or prove attendance.        | Index `(run_id, profile_id, venue_id, status, starts_at, ends_at)`. Seed Anna's Kru Tiger entitlement covering the seeded class period; no booking/access row is created.                                                                         |

### Role, RLS and credential matrix

| Principal            | Provisioning and ownership                                                                                                                | Allowed                                                                                           | Explicitly denied                                                                                                                    |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| Migration connection | Supabase CLI/local owner or separately supplied hosted migration credential; never loaded by application runtime                          | Apply checked-in migrations/seeds, create/alter objects and transfer `app` object ownership       | Browser/runtime use; inclusion in `.env.example` as a usable secret; logging or bundling                                             |
| `app_owner`          | Checked-in `NOLOGIN` role; owns `app` schema objects after migration                                                                      | Object ownership required for controlled migrations                                               | Direct application login and ordinary request execution                                                                              |
| `app_runtime`        | Checked-in `NOLOGIN`, `NOBYPASSRLS`, non-owner group role; an environment-specific login is provisioned out of band and granted this role | Connect, use `app` schema and execute only explicitly granted table operations through forced RLS | DDL, role management, schema ownership, Data API access, bypassing RLS or reading any private row while no policy/context permits it |

Every application table enables and forces RLS in DEV0015. No permissive runtime policy is added yet, so runtime-role queries against private tables return/affect no rows. DEV0016 owns verified subject/run transaction context and the first private policies. DEV0017 owns the separately restricted public catalogue role/path and its safe projections; DEV0015 does not create an unused public repository or anonymous policy.

### Server database module and repository ownership

| Path/capability                                                                                      | Owner                                      | Delivery in this slice                                                                                                                                                                  |
| ---------------------------------------------------------------------------------------------------- | ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `supabase/config.toml`, `supabase/migrations/**`, `supabase/seed.sql`, SQL constraint/RLS/role tests | DEV0015                                    | Yes; sole applied schema and seed history.                                                                                                                                              |
| `src/server/db/env.ts`, `client.ts`, Drizzle table mappings under `src/server/db/schema/**`          | DEV0015                                    | Yes; server-only validated runtime configuration, bounded Postgres.js client (`prepare: false`, one connection for serverless, hosted TLS required) and mappings matching migrated SQL. |
| Client-to-server import guards and `server-only` enforcement                                         | DEV0025                                    | Yes, added alongside the first DEV0015 module; DEV0025 does not own database files.                                                                                                     |
| Auth/profile/run repositories and verified transaction context                                       | DEV0016                                    | No; DEV0015 proves default denial only.                                                                                                                                                 |
| Public catalogue, profile, draft, follow and bookmark repositories/services                          | DEV0017                                    | No; this is the first catalogue read/write owner and maps rows into `src/domain`/response contracts.                                                                                    |
| Booking/attendance, social and later financial repositories                                          | Their owning DEV0018/DEV0023/later tickets | No; add only with their tables and invariants.                                                                                                                                          |

DEV0015 integration tests may issue direct Drizzle queries through the foundation client to prove mappings, role denial and seed contents. That test access is not a production repository. No placeholder service, Route Handler, Server Action, public endpoint or feature repository is created merely to exercise the database.

## Implementation plan

1. Inspect the installed framework and current Supabase/Drizzle tooling; pin compatible CLI/driver packages; create the local Supabase configuration plus only `src/server/db` environment, client and schema-mapping modules. Add server-only configuration validation, hosted TLS, `prepare: false` and a one-connection serverless pool. Coordinate with DEV0025's boundary enforcement without creating a feature repository or assigning database modules to both tickets.
2. Add SQL migrations for the phase-DEV0015 entities in the specification, including `auth.users` references through app profiles, run memberships, organizations/staff/venues, trainer affiliations, class sessions and seeded membership entitlements. Keep wallet binding and activity/financial state for later slices.
3. Add foreign keys, run-scoped uniqueness, typed timestamps/amounts and query indexes; lock down API exposure, schema privileges and default-deny private RLS. Keep migration privileges separate from the runtime role.
   Plan the separate restricted public catalogue read path under C17 for DEV0017; private default-deny rules must not become a blanket login requirement for Explore/Challenges. No browser Data API exposure is needed.
4. Seed exactly the foundation records in the matrix without creating fake Auth identities, bookings, visits or assigning a visitor to Anna. Use stable slugs, explicit fixture provenance and an explicit run schedule anchor; later hosted run tooling chooses a future anchor without mutating the fixed browser preview.
5. Add pgTAP/SQL tests for columns, constraints, foreign keys, indexes, roles and forced RLS plus TypeScript integration tests that exercise the migrated schema through the Drizzle mappings. `supabase db reset` proves clean replay; a second seed execution proves idempotency without a reset. Do not use Drizzle `push` or a second migration journal.
6. Document exact local start/reset/test/stop commands, runtime environment names and safe hosted migration/role-provisioning procedure in README after the commands exist. The preview build must not evaluate database configuration; directly initializing a database module with missing/malformed configuration must fail without revealing credentials.

## Acceptance criteria

- [x] AC1: A fresh disposable local Supabase database applies the exact table/role matrix through the sole SQL migration history; reset plus a second same-database seed execution is deterministic, mappings match the migrated schema and no duplicate seeds appear.
- [x] AC2: Base relationships, same-run constraints, uniqueness and invalid amount/time boundaries are enforced by PostgreSQL, not only form validation.
- [x] AC3: Runtime role has no DDL/table ownership/BYPASSRLS and cannot read private rows without context; browser Data API cannot expose app tables; migration secrets never appear in bundles/logs.
- [x] AC4: Existing frontend preview builds/runs without evaluating database configuration; direct server database initialization fails visibly on missing/malformed configuration or outage rather than returning fabricated state. User-facing database mode remains DEV0017 scope.
- [x] AC5: Actual setup/test commands and redacted environment names are documented; local tests, lint/types/build pass. Any hosted schema applied is identified and verified; no hosted completion is assumed from local tests.

## Validation plan

Use a disposable local database for clean apply, upgrade/reapply, same-database seed idempotency, actual runtime-role denial and invalid-FK/unique/check cases from every matrix row. Query PostgreSQL catalogues in SQL tests to verify expected columns, constraints, indexes, ownership and RLS; exercise representative reads/inserts through the Drizzle mappings to catch mapping drift. Exercise the selected driver/pooler mode, missing/outage failure redaction and pooled role behavior. Run existing domain checks, lint, typecheck and build; browser regression applies only if the paired boundary work changes preview composition. Do not reset a shared hosted project or claim database mocks prove its constraints.

## Risks, limitations, and follow-ups

Project/region selection and credentials remain future inputs. SQL reset tests are local-only. Local validation requires Docker disk capacity for the pinned PostgreSQL image; unrelated images and other projects' containers must not be removed implicitly. DEV0015 deliberately starts PostgreSQL without auxiliary Supabase services, so Auth/API/Realtime/Storage health remains for the ticket that enables each service. A running database does not establish user authorization; DEV0016 is required before private runtime endpoints are enabled. Later tables must arrive with their own feature's invariants/tests.

## Implementation record

Planning update, 2026-09-19 ([DEV0019](../frontend/DEV0019-public-discovery-access.md)): C17 explicitly requires public discovery. Base private-data restrictions remain; guest catalogue privileges/services will be introduced and tested in DEV0017.

Planning update, 2026-09-20 ([DEV0025](DEV0025-nextjs-backend-boundary.md)): keep the application backend in the existing Next.js package and establish its server-only/service/repository boundary as the first part of this ticket. The boundary must start with real DEV0015 modules rather than unused scaffolding.

Planning refinement, 2026-09-20 ([DEV0036](../organisatory/DEV0036-explicit-architecture-boundaries.md)): this ticket solely owns `supabase` artifacts and the concrete `src/server/db` foundation. DEV0025 starts alongside it but owns boundary enforcement, dependency checks and thin-adapter rules rather than this ticket's schema, connection, mappings or database-test evidence.

Planning refinement, 2026-09-20: added the exact ten-table column/key/check/index/seed matrix, role/RLS contract and local-first validation method before implementation. Resolved repository ownership prospectively: DEV0015 owns SQL, connection configuration and Drizzle table mappings but no feature repository; DEV0016 owns identity/access repositories and verified context; DEV0017 owns the first catalogue/draft/social repositories; later feature tickets own their repositories. This supersedes the earlier broad reference to DEV0015 foundation repositories without rewriting the historical DEV0036 record. No runtime or database change was made.

Implementation started on 2026-09-20. `package.json`/`package-lock.json` now pin Supabase CLI 2.117.0, Drizzle ORM 0.45.2, Postgres.js 3.4.9 and `server-only` 0.0.1, with explicit local start/reset/seed/test/lint commands. `supabase/config.toml` uses an isolated `5532x` port range because another local Supabase project already owns the default ports; its Data API schema list excludes `app` and automatic public-table exposure is disabled. `.env.example` documents the sole server-only runtime variable without a usable credential.

`supabase/migrations/20260920000100_create_database_foundation.sql` creates the exact ten-table matrix, named constraints/indexes, update triggers, the `app_owner`/`app_runtime` roles, forced default-deny RLS and explicit grants/revocations. Supabase's migration `postgres` role is intentionally not a superuser: implementation therefore validates that the app roles are not superuser/BYPASSRLS, grants the migration role membership for ownership transfer and does not attempt privileged attribute toggles. `supabase/seed.sql` inserts stable fixture IDs for six unclaimed profiles, one run, four organizations/venues, three classes and Anna's membership using conflict-safe inserts; a second execution inserted zero rows.

`src/server/db/config.ts`, `env.ts`, `client.ts` and `schema/foundation.ts` add redacted lazy URL validation, local-versus-hosted TLS selection, a one-connection Postgres.js client with prepared statements disabled and typed Drizzle mappings. There is deliberately no feature repository, Route Handler, Server Action or product database mode. `supabase/README.md` and the root README document local operations, roles, credentials, hosted safety and the current preview/database boundary.

Final validation replaced the obsolete `supabase seed --local` script—which Supabase CLI 2.117.0 treats as a help-only command—with `scripts/seed-local-database.mjs`. The replacement reads the sole checked-in seed and targets only the fixed loopback development port through Postgres.js; it cannot accept a hosted connection. `db:start` now excludes Auth, Realtime, Storage, Studio, the Data API and other auxiliary containers because DEV0015 needs PostgreSQL only. This also lets the project coexist with another active Supabase stack without making unrelated service health part of database acceptance.

Validation code now lives in `supabase/tests/database/foundation.test.sql`, `tests/database/foundation.test.ts`, `tests/database-config.test.ts` and the expanded boundary test. The SQL test queries PostgreSQL catalogues, roles, privileges, RLS, seeds, foreign keys, triggers and indexes and exercises negative amount/time, duplicate and cross-run constraint cases. The TypeScript integration reads real seeded rows through Drizzle and proves runtime RLS/DDL denial and amount enforcement.

## Validation results

- **Planning consistency — passed:** the ten tables match the DEV0015 logical group in the MVP specification; every column records nullability, and entity versus relationship primary-key conventions, run-scoped foreign keys, indexes, seeds, roles and default-deny RLS have explicit implementation owners.
- **Repository ownership — passed:** reviewed DEV0015, DEV0016, DEV0017, DEV0018, DEV0023, DEV0025 and COR0001. DEV0015 stops at SQL, database configuration/client modules and Drizzle mappings; each feature ticket owns its repositories/services; DEV0025 owns enforcement only.
- **Dependency/security baseline — passed:** `npm install --save-exact drizzle-orm postgres server-only` and `npm install --save-dev --save-exact supabase` completed with zero reported vulnerabilities. `npm test` passed 23/23 tests, including URL redaction/TLS and server boundary checks; `npm run typecheck` passed.
- **Migration and seed against PostgreSQL 17.6 — passed with a noted execution path:** the project-local Supabase database container initialized on `127.0.0.1:55322`; applying the checked-in migration with `psql -v ON_ERROR_STOP=1` created all objects. The first and second seed executions inserted `6/1/6/4/3/4/1/3/3/1` and then zero rows respectively.
- **Database tests — passed:** after a clean reset and second same-database seed, `npm run db:test` passed all 23 expanded pgTAP checks covering exact columns, all 15 foreign keys, ten update triggers, required indexes, roles, privileges, forced RLS, deterministic fixture counts and invalid amount/time/duplicate/cross-run cases. `npm run test:db` passed 5/5 Drizzle integration tests covering mapped fixture reads, runtime-role row/DDL denial, amount enforcement and explicit outage propagation. `npm run db:lint` returned `No schema errors found`.
- **Clean CLI replay — passed after resolving the environment blocker:** after free space increased from about 1.2 GiB to 14 GiB, every required image downloaded successfully. A first full-stack boot applied the migration/seed but timed out waiting for unrelated auxiliary services while another Supabase project was active. The documented database-only `npm run db:start` then passed from both fresh and stopped states; `npm run db:reset` recreated the database and replayed `20260920000100_create_database_foundation.sql` plus `seed.sql`; the corrected `npm run db:seed` reapplied the seed on the same database; and the expanded SQL/Drizzle suites passed. The pre-existing project on ports `5432x` remained running and untouched.
- **Application checks — passed with a builder limitation:** `npm run lint` and `npm run typecheck` passed. `npm run build` reached a Turbopack environment failure because its CSS worker could not bind an internal local port, including outside the managed sandbox; `npx next build --webpack` then compiled, typechecked and generated all 14 routes successfully without `DATABASE_URL`. A scan of `.next/static` found no database URL, role, Drizzle or Postgres.js markers. `npm run test:e2e` passed all 28 desktop/mobile scenarios in 3.3 minutes with the database stopped. Browser database behavior is not applicable because DEV0015 introduces no endpoint or UI mode.
- **Final repository checks — passed:** `npm test` passed 23/23, `npm run lint`, `npm run typecheck` and `npm run format:check` passed, Prettier accepted the updated Markdown, `npx next build --webpack` generated all 14 routes, `.next/static` contained no database URL/role/driver markers and `git diff --check` reported no whitespace errors. The previously completed browser run remains 28/28; no UI, route or browser behavior changed during the final database-only validation.

## Completion and review references

- Completed: 2026-09-20.
- Commit: Not created for implementation.
- Review: Implementation self-review completed; no independent review.
- Deployment: None.
